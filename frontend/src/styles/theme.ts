export interface ThemeInterface {
  colors: {
    [key: string]: string;
    black: string;
  };
}

export const theme = {
  colors: {
    black: '#000',
    greyDDD: '#DDD',
    white: '#FFF',
  },
};
export default theme;
